---
"kilo-code": minor
---

Add a configurable shell executable for the inline terminal, and use the effective inline-terminal shell consistently in command execution and system-prompt shell reporting.
